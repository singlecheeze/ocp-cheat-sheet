apiVersion: v1
kind: Service
metadata:
  name: svc-postgres-nodeport
  namespace: percap
spec:
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
    nodePort: 30000
  selector:
    externalService: svc-postgres-nodeport
  type: NodePort